//listas 
let lista = ["otavio", "alo"]
console.log (lista[0]+" "+ lista[1]) // concactnação ( junção de duas informações de uma lista)
// arrays, objetos
let meta = {
    value: "ler um livro por mês", 
    checked: false,
    log: (info) => {
        console.log(info)
        isChecked(info) =>{

        }
    }
}
meta.value = "não é mais ler um livro"
meta.log(meta.value)
meta.log(meta.checked)
meta.isChecked
// function // arrow function

//const criarMeta = () => {}

    // function criarMeta () {}
